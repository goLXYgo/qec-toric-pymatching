[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "qec-gen"
version = "0.1.0"
description = "Shared Stim and PyMatching tools for quantum error-correcting codes"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "stim",
    "pymatching",
    "pandas",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "matplotlib",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
